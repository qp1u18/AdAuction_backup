/*package frontend.widget;

import backend.Common;
import frontend.DataPoint;

import java.time.LocalDateTime;
import java.util.*;

public class WeekListData extends MetaData {

    private List<String> metricList;
    private HashMap<String, ArrayList<DataPoint>> listData;
    private Common.Interval hourDay;

    public WeekListData (int campaignId, LocalDateTime from, LocalDateTime to, Common.Interval interval, Map<String, EnumSet<?>> filters, HashMap<String,Number> widgetData, List<String> metricList, HashMap<String, ArrayList<DataPoint>> listData, Common.Interval hourDay) {
        super(campaignId, from, to, interval, filters, widgetData);
        this.metricList = metricList;
        this.listData = listData;
        this.hourDay = hourDay;
    }



}*/
